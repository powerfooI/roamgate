#!/usr/bin/env sh
set -eu

# Git exports relative GIT_* paths during commit; keep them out of the
# nested git commands that tests run in their own repositories.
unset GIT_DIR GIT_INDEX_FILE GIT_WORK_TREE

bun run precommit
